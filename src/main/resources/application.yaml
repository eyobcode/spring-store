spring:
  application:
    name: spring-store
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/storedb?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: myPassword!
  jpa:
    show-sql: true  # Enables logging of SQL statements to the console
    properties:
      hibernate:
#        format_sql: true  # Pretty-prints the SQL for better readability
        highlight_sql: true # Optional: adds syntax highlighting if your console supports it

#    driver-class-name: com.mysql.cj.jdbc.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: update
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.MySQL8Dialect